#bootstrap.yml可以理解成系统级别的一些参数配置，这些参数一般是不会变动的
#服务名称
spring: 
  application: 
    name: user-api

server: 
  port: 2222

#注册到服务中心
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

